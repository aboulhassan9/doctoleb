# DoctoLeb Top 40 To 80 Percent Fix Pass

Date: 2026-05-09
Scope: next production-hardening band after the top-40 deploy/security gates.

## Completed In This Slice

| Area | Change | Evidence |
| --- | --- | --- |
| Provider-aware tenant draft | `admin-create-provisioning-job` now accepts `automationMode`, `supabaseConnectionId`, and `vercelConnectionId`, validates UUIDs, and forwards them to the service-role RPC. | `supabase-control-plane/functions/admin-create-provisioning-job/index.ts`; unit contract. |
| Undoable step ledger | New control-plane migration seeds 10 provisioning steps for every new tenant draft: draft, provider selection, Supabase project, migrations, tenant profile, first doctor/admin, Vercel routing, runtime config, resolver smoke, activation. | `supabase-control-plane/migrations/00010000000015_control_plane_provider_aware_draft_steps.sql`; rollback live smoke. |
| Provider connection UI | SaaS console can create/archive provider connection metadata and rejects raw token-shaped secret references before API calls. | `apps/control-plane/src/components/ProviderConnectionsPanel.jsx`; helper tests. |
| Draft creation UI | New tenant draft form can select manual, assisted, or automatic mode and choose active automation-ready Supabase/Vercel connections. | `apps/control-plane/src/components/ProvisioningPanel.jsx`; helper tests. |
| Provisioning visibility | Tenant detail now returns and renders `tenant_provisioning_steps` so the SaaS admin sees exact readiness and undo state. | `admin-get-tenant`, `ProvisioningStepsPanel.jsx`. |
| Live control-plane deployment | Applied migration to `xouqxgwccewvbtkqming`; redeployed `admin-create-provisioning-job` and `admin-get-tenant`. | Supabase MCP migration list and Edge Function list. |
| Provisioning step runner | Added `admin-run-provisioning-step` plus private service-role RPCs to execute safe internal ledger checks one step at a time. It verifies provider credentials from server-side secret references and refuses external creation steps instead of fake-completing them. | `00010000000016_control_plane_provisioning_step_runner.sql`, `providerExecution.ts`, `admin-run-provisioning-step`, console `Run safe check`. |
| Provider secret reference alignment | Automation-enabled provider connections now require uppercase Edge Function secret names in both console validation and server validation, matching what the runner can resolve. | `providerConnectionDrafts.js`, `_shared/providerConnections.ts`; provider admin functions redeployed. |
| Operator-linked Supabase project check | `create_supabase_project` no longer fake-completes. It succeeds only when an operator has stored a valid tenant project ref, URL, and anon key in runtime config, then records the external Supabase project identity for later compensation. | `admin-run-provisioning-step`, live redeploy, unit contract. |
| Tenant migration readiness check | `apply_tenant_migrations` verifies the linked tenant DB has the expected runtime tables through a tenant service-role secret reference before later seed steps can run. | `admin-run-provisioning-step`, live redeploy, unit contract. |
| Doctor-independent tenant profile seed | Tenant DB now has `service_seed_tenant_profile(text,text,jsonb)`, executable only by `service_role`, and `tenant_profile.doctor_id` is nullable so the SaaS provisioning flow can seed tenant branding/app config before creating the first doctor/admin. | `supabase/migrations/20260509022000_tenant_profile_seed_service.sql`, live tenant migration, rollback seed proof. |
| Shared branding normalization | Control-plane config sync and provisioning seed now use one shared normalizer for display name, colors, locales, support fields, and HTTPS-only image URLs. | `_shared/tenantBranding.ts`, `admin-sync-tenant-config`, `admin-run-provisioning-step`. |
| First doctor/admin input contract | New tenant draft creation now requires first doctor/admin name/email, stores it in the zero-PHI control-plane provisioning job, and exposes it to the runner through explicit select fields. | `00010000000017_control_plane_first_doctor_admin_inputs.sql`, `ProvisioningPanel.jsx`, `admin-create-provisioning-job`. |
| First doctor/admin tenant seed | Tenant DB now has `service_seed_first_doctor_admin(uuid,text,text,text,uuid)`, executable only by `service_role`. The runner invites the doctor through tenant Auth, seeds the domain `users`/`doctors` records idempotently, links `tenant_profile.doctor_id`, and compensates by deleting the Auth user if tenant DB seeding fails. | `20260509023000_first_doctor_admin_seed_service.sql`, `admin-run-provisioning-step`, live rollback proof. |
| Vercel/free-alias readiness check | `configure_vercel_project` now verifies active patient and ops routable hosts without requiring `doctoleb.com`; localhost and Vercel aliases are accepted, real custom domains still need verified DNS/SSL. | `admin-run-provisioning-step`; unit contract. |
| Resolver smoke and activation | `smoke_test_resolver` proves control-plane resolver mapping before activation, and `activate_tenant` only flips status to active after runtime config plus routable patient/ops domains exist and public resolver smoke passes. | `admin-run-provisioning-step`; `admin_update_tenant_atomic`; unit contract. |
| Provisioning cancel/compensate | New service-role-only RPCs and admin Edge Functions let operators cancel non-final jobs and compensate succeeded steps with undo metadata. Browser code calls named admin APIs only. | `00010000000018_control_plane_provisioning_cancel_compensate.sql`, `admin-cancel-provisioning-job`, `admin-compensate-provisioning-step`, `ProvisioningStepsPanel.jsx`. |

## Verification

| Check | Result |
| --- | --- |
| Focused unit contracts | PASS, `node --test tests/unit/controlPlaneDraftHelpers.test.mjs tests/unit/saasFoundationContracts.test.mjs` |
| Control-plane build | PASS, `npm run build:control-plane` |
| Full repo verification | PASS, `npm run verify`, 179 unit tests; local live DB contract subchecks skipped because `BACKEND_TEST_*` env vars are not present locally |
| Standalone builds | PASS, `npm run build:patient`, `npm run build:ops`, `npm run build:control-plane` |
| Bundle secret scans | PASS for patient, ops, and control-plane build outputs |
| Live rollback DB smoke | PASS, assisted draft created 10 provisioning steps and rolled back |
| Live runner DB smoke | PASS, temporary provisioning step moved `running -> skipped`, wrote safe audit events, and rolled back |
| Live runner function smoke | PASS, `admin-run-provisioning-step` is active with `verify_jwt=true`; unauthenticated POST fails closed; CORS rejects unknown origins; updated function deployed with provider credential verification helper |
| Local browser smoke | PASS, control-plane unauthenticated shell loaded with no console/page errors; screenshot `output/playwright/control-plane-local-login.png` |
| Live tenant seed migration | PASS, applied to `gezmfmskhmjgnquoyosq`; `tenant_profile.doctor_id` is nullable, `service_seed_tenant_profile` exists, anon/authenticated cannot execute it, and rollback seed proof left 0 QA rows. |
| Edge deploy parity | PASS, redeployed `admin-run-provisioning-step` and `admin-sync-tenant-config` after shared branding normalization. |
| First doctor/admin live DB proof | PASS, applied control-plane and tenant migrations; rollback proof created a temporary Auth/user/doctor/link path and rolled it back; live checks show browser roles cannot execute the new RPCs. |
| Final runner deploy | PASS, redeployed `admin-create-provisioning-job` and `admin-run-provisioning-step`; both are active with `verify_jwt=true`. |
| No-domain provisioning contract | PASS, `node --test tests/unit/saasFoundationContracts.test.mjs`, 44/44 SaaS foundation contracts. |
| Control-plane quality gates after cancel/compensate UI | PASS, `npm run lint`; `npm run build:control-plane`. |
| Live cancel/compensate deployment | PASS, migration applied to `xouqxgwccewvbtkqming`; `admin-cancel-provisioning-job` and `admin-compensate-provisioning-step` are ACTIVE with `verify_jwt=true`; updated runner deployed as version 7. |
| Live cancel/compensate security smoke | PASS, new RPCs grant execute only to `postgres`/`service_role`; unauthenticated admin function POSTs fail closed; unknown-origin preflight returns `ORIGIN_NOT_ALLOWED`. |
| Resolver direct-RPC hardening | PASS, `resolve_tenant` direct browser role execute revoked; public resolver remains the `tenant-resolve` Edge Function; tenant resolver smoke still passes. |

## Still Not Done

| Item | Why not done in this slice |
| --- | --- |
| Supabase project creation through Management API | External project creation is still not automatic. Current v1 safely supports operator-linked existing tenant projects; automatic Management API creation still needs a server-side executor, external resource IDs, cost/project-region controls, and compensation. |
| Vercel REST mutation automation | Vercel/free-alias routing can now be verified from the control plane, but creating/changing Vercel projects, env vars, and custom domains through the Vercel API is still deferred until provider credentials, external IDs, and rollback rules are proven. |
| Tenant service-role secret injection | The runner can consume `TENANT_SERVICE_ROLE_KEY_<PROJECT_REF>` secrets, but setting those Edge Function secrets is still a privileged operator action until provider/Vault automation is added. |
| First doctor/admin completed undo | Creation now has compensation on failed seed, and general provisioning compensation exists, but a separate operator-facing "disable/cancel first doctor admin" undo function is still needed for completed tenant onboarding resources. |
| Authenticated browser provisioning flow | Local shell does not have readable control-plane login credentials; GitHub secrets exist but cannot be read back. |
| GitHub push/deploy | Required `BACKEND_TEST_*` secrets are still missing, so pushing to `main` would intentionally fail CI. |

## Next Slice

Run authenticated browser E2E proof for the control-plane provisioning flow, staff invite acceptance/reactivation, and patient/staff first-band workflows. Keep real custom domains pending until owned/verified. Do not add Supabase Management API project creation until external resource compensation, cost controls, and region/org selection are designed and tested.
